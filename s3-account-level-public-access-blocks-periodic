Log in to the AWS Management Console:

Go to the AWS Management Console at https://aws.amazon.com/.
Log in to your AWS account using the appropriate credentials.
Navigate to the S3 Dashboard:

Once you are logged in to the AWS Management Console, go to the Amazon S3 Dashboard.
This can be done by clicking on the "Services" menu, and then selecting "S3" from the list of AWS services.
Configure public access blocks:

On the S3 Dashboard, select the "Permissions" tab.
Then, select "Public access settings."
You can configure public access blocks at the account level by selecting the appropriate settings for the following options:
Block public access to buckets and objects granted through new access control lists (ACLs).
Block public access to buckets and objects granted through any access control lists (ACLs).
Block public and cross-account access to buckets and objects through any public bucket policies.
Save the public access block configuration:

After you have selected the appropriate settings, click the "Save" button to apply the changes.
This will complete the remediation of the "S3-account-level-public-access-blocks-periodic" issue.
